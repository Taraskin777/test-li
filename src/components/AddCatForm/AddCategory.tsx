import React from 'react';
import styles from './addCatForm.module.scss';

const AddCategory: React.FC = (): JSX.Element => {
  return (
    <div>
      <button>Add Category</button>
    </div>
  );
};

export default AddCategory;
